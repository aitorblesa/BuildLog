---
interface Props {
  class?: string;
  signal?: boolean;
}
const { class: className = '', signal = false } = Astro.props;
---
<span class:list={['font-display font-bold', signal ? 'text-signal' : 'text-ink', className]}>
  <slot />
</span>
