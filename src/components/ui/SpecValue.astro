---
interface Props {
  class?: string;
  signal?: boolean;
}
const { class: className = '', signal = false } = Astro.props;
---
<span class:list={['font-display font-bold', signal ? 'text-signal-ink' : 'text-ink', className]}>
  <slot />
</span>
