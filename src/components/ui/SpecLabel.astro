---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class:list={['font-mono text-[12px] uppercase tracking-[0.14em] text-muted', className]}>
  <slot />
</span>
